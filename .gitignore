# Secrets — never commit
android/local.properties
*.jks
*.keystore

# Build output
android/.gradle/
android/build/
android/app/build/
android/captures/
.gradle/
build/

# IDE
.idea/
*.iml
.DS_Store

# Independently version-controlled elsewhere (shishirkush/telecalling-dashboard),
# deployed straight to GitHub Pages from its own repo.
dashboard/

# Supabase CLI's local cache — project ref, org id, and pooler hostname,
# regenerated by `supabase link`/`supabase login`. Not a secret by itself
# (no password in it) but it's local machine state, not source.
supabase/.temp/
